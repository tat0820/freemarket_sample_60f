= render "shared/top_header"

.top-image

.top-category
  .top-category__title
    人気のカテゴリー
  .top-category__kind
    .top-category__kind__name
      レディース
    .top-category__kind__name
      メンズ
    .top-category__kind__name
      家電・スマホ・カメラ
    .top-category__kind__name
      おもちゃ・ホビー・グッズ

.top-products
  .top-products__title
    全てのアイテム
  .top-products__show
    もっと見る＞
  .top-products__items
    - @products.each do |product|

      = link_to product_path(product.id) do
        .top-products__items__item
          = image_tag product.images[0].img
          .top-products__items__item__price
            ¥
            = product.price
          .top-products__items__item__name
            = product.name

.top-icon
  = link_to "/products/new" do
    .top-icon__margin
    .top-icon__text
      出品
    = fa_icon "camera" 

= render "shared/top_footer"


